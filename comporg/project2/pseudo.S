

main:
// initializations, storing registers on stack, etc.

readArgs:
// recursively reads in all args, branching to createNode or delete for each arg.

insert:
// inserts created nodes into structure

delete:
// deletes any negative inputs

createNode: